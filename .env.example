# Copy to `.env` and adjust. Loaded automatically by the CLI via python-dotenv.

# Warehouse connection. Default matches docker-compose.yml.
# For local testing without Docker you can point this at SQLite:
#   MILL_DB_URL=sqlite:///mill.db
MILL_DB_URL=postgresql+psycopg://mill:mill@localhost:5433/mill
